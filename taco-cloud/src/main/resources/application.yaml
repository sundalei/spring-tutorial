spring:
  datasource:
    url: jdbc:mysql://localhost/tacocloud4
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    rest:
      base-path: /api

logging:
  level:
    tacos: warn